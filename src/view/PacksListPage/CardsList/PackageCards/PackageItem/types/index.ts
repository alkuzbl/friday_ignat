export type PackageItemForMePropsType = {
  question: string;
  answer: string;
  date: string;
  grade: number;
  index: number;
  cardId: string;
  packId: string | undefined;
  isMyCards: boolean;
};
